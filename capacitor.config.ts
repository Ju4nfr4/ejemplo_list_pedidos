import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.prueba.ionicpush',
  appName: 'ejemplo_list_pedidos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
